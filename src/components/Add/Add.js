import './Add.scss'
import React, { Component } from 'react'
import PropTypes from 'prop-types'
class Add extends Component {
  constructor(props) {
    super(props)
  }
  render(){
    return()
  }
}
Add.propTypes = {
}
export default Add